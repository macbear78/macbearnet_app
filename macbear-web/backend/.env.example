PORT=3000
NODE_ENV=development

# MariaDB — match docker-compose defaults for local dev
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=macbear
DB_PASSWORD=macbear_dev
DB_NAME=macbear

# CORS: Vue dev server (Vite)
CORS_ORIGIN=http://127.0.0.1:5173
